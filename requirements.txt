# Extraction — notebooks/data_extraction.ipynb, Workbench only.
google-cloud-bigquery>=3.11.0
db-dtypes>=1.1.0        # BigQuery needs it for .to_dataframe()
pyarrow>=12.0.0         # the Arrow path behind .to_dataframe()

# Analysis — src/, notebooks/analysis.ipynb.
pandas>=3.0
numpy>=2.5
scikit-learn>=1.9
matplotlib>=3.11
seaborn>=0.13

# Notebooks
jupyter>=1.1
ipykernel>=7.3
ipython>=9.15           # IPython.display.display, used in both notebooks

# Dashboard — see dashboard/requirements.txt for the deployed environment
streamlit>=1.60
