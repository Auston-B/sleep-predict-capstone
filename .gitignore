# Environment
.env
*.env
dashboard/.streamlit/secrets.toml

# Data — NEVER commit participant data
*.csv
*.parquet
*.feather
*.pkl
data/

# Exception — the aggregate dashboard tables are the only CSVs published.
# synthetic_data/*.csv is deliberately NOT excepted: regenerate it with generate.py.
!dashboard/*.csv

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/
env/

# Tooling caches
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Local scratch — Workbench replication drafts, not for publication
workbench_env/

# Kept locally, not published — regeneration tooling and internal memos
scripts/
reports/analysis_corrections.md

# fig_ai_workflow.png was ignored while the AI statement was a separate local
# file. That statement is now Appendix C of final_report.md, which is published,
# so the figure has to ship with it or Figure A6 renders broken.

# Built by .github/workflows/deploy-pages.yml
_site/

# OS
.DS_Store
Thumbs.db
.Rhistory
.RData

# IDE
.vscode/
.idea/
